name: build

on:
  push:
    branches:
      - dev
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  vehicle:
    uses: ./.github/workflows/ci-vehicle.yml

  vehicle-python:
    needs: [vehicle]
    uses: ./.github/workflows/ci-vehicle-python.yml
