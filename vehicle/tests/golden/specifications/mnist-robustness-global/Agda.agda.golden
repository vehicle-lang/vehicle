-- WARNING: This file was generated automatically by Vehicle
-- and should not be modified manually!
-- Metadata:
--  - Vehicle version: 0.28.0+dev
--  - Agda version: 2.6.2

{-# OPTIONS --allow-exec #-}

open import Vehicle
open import Vehicle.Utils
open import Vehicle.Data.Tensor as 𝕋
open import Data.Product
open import Data.Rational as ℚ using (ℚ)
open import Data.Fin as Fin using (Fin; #_)
open import Data.List.Base
open import Data.Vec.Functional renaming ([] to []ᵥ; _∷_ to _∷ᵥ_)
open import Function.Base

module Agda where

Image : Set
Image = Tensor ℚ (28 ∷ (28 ∷ []))

Score : Set
Score = Tensor ℚ (10 ∷ [])

postulate classifier : Image → Score

ValidImage : Image → Set
ValidImage x = ∀ i → ∀ j → (natScalar 0) ≤ ((x ! i) ! j) × ((x ! i) ! j) ≤ (natScalar 1)

postulate epsilon : Tensor ℚ []

postulate delta : Tensor ℚ []

CloseInputs : Image → Image → Set
CloseInputs x y = ∀ i → ∀ j → 𝕋.- epsilon ≤ ((x ! i) ! j) 𝕋.- ((y ! i) ! j) × ((x ! i) ! j) 𝕋.- ((y ! i) ! j) ≤ epsilon

CloseOutputs : Score → Score → Set
CloseOutputs a b = ∀ i → 𝕋.- delta ≤ (a ! i) 𝕋.- (b ! i) × (a ! i) 𝕋.- (b ! i) ≤ delta

StandardRobustAround : Image → Set
StandardRobustAround y = ∀ x → ValidImage x × CloseInputs x y → CloseOutputs (classifier x) (classifier y)

postulate trainingImages : Vector Image 2

postulate standardRobust : StandardRobustAround (trainingImages $ # 0)