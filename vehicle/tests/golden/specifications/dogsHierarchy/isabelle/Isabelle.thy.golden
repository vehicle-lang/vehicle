(* WARNING: This file was generated automatically by Vehicle *)
(* and should not be modified manually! *)
(* Metadata: *)
(*  - Vehicle version: 0.27.0+dev *)
(*  - Isabelle version: 2024 *)

theory  Isabelle

  imports

    "Complex_Main"

    "Deep_Learning.Tensor"
    "Deep_Learning.Tensor_Subtensor"
    "Vehicle.Vehicle"

begin

  type_synonym R = "real"

  

  definition numberOfDogs  :: "  nat "
    where " numberOfDogs   = ( (6 :: nat) ) "

  typedef Dog  =  "{ i :: nat. i < (numberOfDogs) }"
  unfolding numberOfDogs_def
    by (simp, rule_tac x = "0" in exI, linarith)

  (* Type Coercions *)
  declare [[coercion Rep_Dog]]
  definition to_Dog :: "FlexIndex \<Rightarrow> Dog"
    where[simp]: "to_Dog a = (
      let i = Rep_FlexIndex a
      in (if i < numberOfDogs then Abs_Dog i else undefined))"
  declare [[coercion to_Dog]]

  typedef Image  =  "{ a :: R tensor. (dims a) = ((4 :: nat) # ((4 :: nat) # [])) }"
    using dims_tensor_from_lookup by blast

  (* Type Coercions *)
  declare [[coercion Rep_Image]]
  definition to_Image :: "R FlexTensor \<Rightarrow> Image"
    where[simp]: "to_Image a = (
      let t = Rep_FlexTensor a
      in (if dims t = ((4 :: nat) # ((4 :: nat) # [])) then Abs_Image t else undefined))"
  declare [[coercion to_Image]]
  (* Type Rewrite Rules *)
  lemma Image_tensor_rewrite0[simp]:
    assumes "prod_list shape = length elems"
        and "shape = (4 :: nat) # ((4 :: nat) # [])"
    shows "(Rep_tensor (Rep_Image (Abs_Image (Abs_tensor (shape,elems))))) =  (shape,elems)"
  proof -
    have "Rep_Image (Abs_Image (Abs_tensor (shape,elems)))
      = Abs_tensor (shape,elems)"
        using Abs_Image_inverse[of "Abs_tensor (shape,elems)"]
        using Abs_tensor_inverse[of "(shape, elems)"]
        unfolding dims_def
        using assms
        by (simp)
    moreover have "Rep_tensor (Abs_tensor (shape,elems)) = (shape,elems)"
        using assms
        by (simp add: Abs_tensor_inverse)
    ultimately show ?thesis by simp
  qed

  typedef Score  =  "{ a :: R tensor. (dims a) = ([]) }"
    using dims_tensor_from_lookup by blast

  (* Type Coercions *)
  declare [[coercion Rep_Score]]
  definition to_Score :: "R FlexTensor \<Rightarrow> Score"
    where[simp]: "to_Score a = (
      let t = Rep_FlexTensor a
      in (if dims t = ([]) then Abs_Score t else undefined))"
  declare [[coercion to_Score]]
  (* Type Rewrite Rules *)
  lemma Score_tensor_rewrite0[simp]:
    assumes "prod_list shape = length elems"
        and "shape = []"
    shows "(Rep_tensor (Rep_Score (Abs_Score (Abs_tensor (shape,elems))))) =  (shape,elems)"
  proof -
    have "Rep_Score (Abs_Score (Abs_tensor (shape,elems)))
      = Abs_tensor (shape,elems)"
        using Abs_Score_inverse[of "Abs_tensor (shape,elems)"]
        using Abs_tensor_inverse[of "(shape, elems)"]
        unfolding dims_def
        using assms
        by (simp)
    moreover have "Rep_tensor (Abs_tensor (shape,elems)) = (shape,elems)"
        using assms
        by (simp add: Abs_tensor_inverse)
    ultimately show ?thesis by simp
  qed

  locale  Isabelle  = 

    fixes  score  :: " Image \<Rightarrow> R FlexTensor "

    fixes  greatDane  :: " FlexIndex "
    fixes  germanShepherd  :: " FlexIndex "
    fixes  chihuahua  :: " FlexIndex "
    fixes  pekinese  :: " FlexIndex "
    fixes  smallDogs  :: " Dog list "
    fixes  bigDogs  :: " Dog list "
    fixes  validPixel  :: " (R tensor) \<Rightarrow> bool "
    fixes  validImage  :: " (Image) \<Rightarrow> bool "
    fixes  isFirstChoice  :: " (Image) \<Rightarrow> (Dog) \<Rightarrow> bool "
    fixes  isSecondChoice  :: " (Image) \<Rightarrow> (Dog) \<Rightarrow> bool "
    fixes  noConfusionWith  :: " (Image) \<Rightarrow> (Dog list) \<Rightarrow> (Dog list) \<Rightarrow> bool "

    defines  greatDane_def: " greatDane \<equiv> (Abs_FlexIndex 1) "
    defines  germanShepherd_def: " germanShepherd \<equiv> (Abs_FlexIndex 2) "
    defines  chihuahua_def: " chihuahua \<equiv> (Abs_FlexIndex 4) "
    defines  pekinese_def: " pekinese \<equiv> (Abs_FlexIndex 5) "
    defines  smallDogs_def: " smallDogs \<equiv> chihuahua # (pekinese # []) "
    defines  bigDogs_def: " bigDogs \<equiv> greatDane # (germanShepherd # []) "
    defines  validPixel_def: " validPixel \<equiv> \<lambda> p . ((leqTensorReduced (flextensor_from_vec [] [ (((0 :: R) )) ]) p)) \<and> ((leqTensorReduced p (flextensor_from_vec [] [ (((1 :: R) )) ]))) "
    defines  validImage_def: " validImage \<equiv> \<lambda> x . (forallIndex (4 :: nat)  (\<lambda>  i . ((forallIndex (4 :: nat)  (\<lambda>  j . ((validPixel (flex_subtensor ((flex_subtensor x i)) j)))))))) "
    defines  isFirstChoice_def: " isFirstChoice \<equiv> \<lambda> x dog1 . let scores = (score x) in (forallIndex (6 :: nat)  (\<lambda>  d . (d \<noteq> dog1 \<longrightarrow> (gtTensorReduced (flex_subtensor scores dog1) (flex_subtensor scores d))))) "
    defines  isSecondChoice_def: " isSecondChoice \<equiv> \<lambda> x dog2 . let scores = (score x) in (existsIndex (6 :: nat)  (\<lambda>  dog1 . ((isFirstChoice x dog1) \<and> (forallIndex (6 :: nat)  (\<lambda>  d . ((d \<noteq> dog1) \<and> (d \<noteq> dog2) \<longrightarrow> (gtTensorReduced (flex_subtensor scores dog2) (flex_subtensor scores d)))))))) "
    defines  noConfusionWith_def: " noConfusionWith \<equiv> \<lambda> x dogs1 dogs2 . (forallInList (\<lambda>  dog1 . ((forallInList (\<lambda>  dog2 . (\<not> (isFirstChoice x dog1) \<and> (isSecondChoice x dog2))) dogs2))) dogs1) "

    assumes  doesNotConfuseBigAndSmall :  " (\<forall> x. (validImage x) \<longrightarrow> (noConfusionWith x bigDogs smallDogs)) "

    begin

    end

end