@property;
expandedExpr : forallT (f : Tensor -> Tensor) . Tensor;
expandedExpr f = forall x . x ! 0 >= f x ! 0

@property;
sequential : forallT (f : Tensor -> Tensor) . Tensor;
sequential f = forall x . forall y . f x ! 0 >= f y ! 0

@property;
parallel : forallT (f : Tensor -> Tensor) . Tensor;
parallel f = (forall x . f x ! 0 >= 0.0) and (exists x . f x ! 0 >= 5.0)