[
  {
    "name": "TypeCheck",
    "run": "vehicle compile -s simple-foreach.vcl -t TypeCheck",
    "needs": ["simple-foreach.vcl"]
  },
  {
    "name": "Marabou",
    "run": "vehicle compile -s simple-foreach.vcl -t Marabou -o simple-foreach.inputquery/ --network f:NO_FILE",
    "needs": ["simple-foreach.vcl"],
    "produces": ["simple-foreach.inputquery/*.txt", "simple-foreach.inputquery/verification-plan.vcle"]
  },
  {
    "name": "LossFunction",
    "run": "vehicle compile -s simple-foreach.vcl -t LossFunction -o simple-foreach.loss.json --network f:NO_FILE",
    "needs": ["simple-foreach.vcl"],
    "produces": ["simple-foreach.loss.json"]
  }
]
