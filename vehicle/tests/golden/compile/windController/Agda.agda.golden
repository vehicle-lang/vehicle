-- WARNING: This file was generated automatically by Vehicle
-- and should not be modified manually!
-- Metadata:
--  - Agda version: 2.6.2
--  - Vehicle version: 0.16.0+dev

{-# OPTIONS --allow-exec #-}

open import Vehicle
open import Vehicle.Utils
open import Vehicle.Data.Tensor
open import Data.Product
open import Data.Integer as ℤ using (ℤ)
open import Data.Rational as ℚ using (ℚ)
open import Data.Fin as Fin using (Fin; #_)
open import Data.Vec.Functional.Relation.Unary.All as Fin
open import Data.List.Base

module Agda where

InputVector : Set
InputVector = Tensor ℚ (2 ∷ [])

currentSensor : Fin 2
currentSensor = # 0

previousSensor : Fin 2
previousSensor = # 1

OutputVector : Set
OutputVector = Tensor ℚ (1 ∷ [])

velocity : Fin 1
velocity = # 0

postulate controller : InputVector → OutputVector

normalise : InputVector → InputVector
normalise x = foreach (λ i → ((x ! i) 𝕋.⊕ 4) 𝕋.÷ 8)

SafeInput : InputVector → Set
SafeInput x = Fin.All (λ i → 𝕋.- (ℤ.+ 13 / 4) ≤ (x ! i) × (x ! i) ≤ ℤ.+ 13 / 4)

SafeOutput : InputVector → Set
SafeOutput x = let y = controller (normalise x) ! velocity in 𝕋.- (ℤ.+ 5 / 4) < (y 𝕋.⊕ 2 𝕋.* (x ! currentSensor)) 𝕋.⊖ (x ! previousSensor) × (y 𝕋.⊕ 2 𝕋.* (x ! currentSensor)) 𝕋.⊖ (x ! previousSensor) < ℤ.+ 5 / 4

postulate safe : ∀ x → SafeInput x → SafeOutput x