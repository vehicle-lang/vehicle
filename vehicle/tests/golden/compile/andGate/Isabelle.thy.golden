(* WARNING: This file was generated automatically by Vehicle *)
(* and should not be modified manually! *)
(* Metadata: *)
(*  - Isabelle version: 2024 *)
(*  - Vehicle version: 0.21.0+dev *)

theory  Isabelle

  imports

    "Complex_Main"

    "Deep_Learning.Tensor"
    "Deep_Learning.Tensor_Subtensor"
    "Vehicle.Vehicle"

begin

  type_synonym R = "real"

  

  

  definition truthy  :: " (R tensor \<Rightarrow> R FlexTensor) \<Rightarrow> (R tensor) \<Rightarrow>  bool "
    where " truthy   andGate x = ( geqTensorReduced x (flextensor_from_vec [] [ (((1 :: R) / 2)) ]) ) "

  definition falsey  :: " (R tensor \<Rightarrow> R FlexTensor) \<Rightarrow> (R tensor) \<Rightarrow>  bool "
    where " falsey   andGate x = ( leqTensorReduced x (flextensor_from_vec [] [ (((1 :: R) / 2)) ]) ) "

  definition validInput  :: " (R tensor \<Rightarrow> R FlexTensor) \<Rightarrow> (R tensor) \<Rightarrow>  bool "
    where " validInput   andGate x = ( (leqTensorReduced (flextensor_from_vec [] [ (((0 :: R) )) ]) x) \<and> (leqTensorReduced x (flextensor_from_vec [] [ (((1 :: R) )) ])) ) "

  definition correctOutput  :: " (R tensor \<Rightarrow> R FlexTensor) \<Rightarrow> (R tensor) \<Rightarrow> (R tensor) \<Rightarrow>  bool "
    where " correctOutput   andGate x1 x2 = ( let y = (subtensor ((andGate (combine_subtensors [ x1,x2]))) 0) in ((truthy andGate x1) \<and> (truthy andGate x2) \<longrightarrow> (truthy andGate y)) \<and> (((truthy andGate x1) \<and> (falsey andGate x2) \<longrightarrow> (falsey andGate y)) \<and> (((falsey andGate x1) \<and> (truthy andGate x2) \<longrightarrow> (falsey andGate y)) \<and> ((falsey andGate x1) \<and> (falsey andGate x2) \<longrightarrow> (falsey andGate y)))) ) "



  locale  Isabelle  = 

    fixes  andGate  :: " R tensor \<Rightarrow> R FlexTensor "

    assumes  andGateCorrect :  " \<forall> x1. \<forall> x2. (validInput andGate x1) \<and> (validInput andGate x2) \<longrightarrow> (correctOutput andGate x1 x2) "

    begin

    end

end