-- WARNING: This file was generated automatically by Vehicle
-- and should not be modified manually!
-- Metadata:
--  - Vehicle version: 0.27.0+dev
--  - Agda version: 2.6.2

{-# OPTIONS --allow-exec #-}

open import Vehicle
open import Vehicle.Utils
open import Vehicle.Data.Tensor as 𝕋
open import Data.Product
open import Data.Rational as ℚ using (ℚ)
open import Data.Fin as Fin using (Fin; #_)
open import Data.List.Base

module Agda where

postulate f : Tensor ℚ (2 ∷ []) → Tensor ℚ (3 ∷ (2 ∷ []))

trajectory : Tensor ℚ (3 ∷ (2 ∷ []))
trajectory = f (natScalar 0∷ᵥnatScalar 0∷ᵥ[]ᵥ)

positions : Tensor ℚ (3 ∷ [])
positions = transpose trajectory ! # 0

postulate positionsInRange : ∀ t → (natScalar 0) ≤ (positions ! t) × (positions ! t) ≤ (natScalar 10)

postulate positionUnderLimit : positions ≤ constTensor (natScalar 10) (3 ∷ [])

postulate boundedScalar : ∃ λ v → ((natScalar 0) ≤ v × v ≤ (natScalar 1)) × ∀ t → (positions ! t) ≥ v