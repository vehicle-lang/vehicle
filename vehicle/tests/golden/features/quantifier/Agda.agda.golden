-- WARNING: This file was generated automatically by Vehicle
-- and should not be modified manually!
-- Metadata:
--  - Vehicle version: 0.27.1+dev
--  - Agda version: 2.6.2

{-# OPTIONS --allow-exec #-}

open import Vehicle
open import Vehicle.Utils
open import Vehicle.Data.Tensor as 𝕋
open import Data.Unit
open import Data.Product
open import Data.Rational as ℚ using (ℚ)
open import Data.List.Base

module Agda where

unused : Set
unused = ∀ ((x : Tensor ℚ [])) → ⊤

postulate f : Tensor ℚ [] → Tensor ℚ []

postulate expandedExpr : ∀ x → (natScalar 0) < x × x < (natScalar 1) → x ≥ f x

postulate parallel : ∀ x → (natScalar 0) < x × x < (natScalar 1) → f x ≥ (natScalar 0) × ∃ λ y → ((natScalar 0) < y × y < (natScalar 1)) × f y ≥ (natScalar 5)