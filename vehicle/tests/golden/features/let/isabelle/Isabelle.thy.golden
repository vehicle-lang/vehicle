(* WARNING: This file was generated automatically by Vehicle *)
(* and should not be modified manually! *)
(* Metadata: *)
(*  - Vehicle version: 0.27.1+dev *)
(*  - Isabelle version: 2024 *)

theory  Isabelle

  imports

    "Complex_Main"

    "Deep_Learning.Tensor"
    "Vehicle.Vehicle"

begin

  type_synonym R = "real"

  

  

  locale  Isabelle  = 

    fixes  e3  :: " nat "
    fixes  lamLet  :: " nat \<Rightarrow> nat "
    fixes  letLam  :: " nat \<Rightarrow> nat "
    fixes  falsey  :: " (R tensor) \<Rightarrow> bool "

    defines  e3_def: " e3 \<equiv> let y = ((1 :: nat) + (1 :: nat)) in y "
    defines  lamLet_def: " lamLet \<equiv> \<lambda>  (x :: nat) . (let y = x in y) "
    defines  letLam_def: " letLam \<equiv> let y = \<lambda>  x . (x) in y "
    defines  falsey_def: " falsey \<equiv> \<lambda> x . (leqTensorReduced x (flextensor_from_vec [] [ (((1 :: R) / 2)) ])) "

    assumes  e1 :  " let x = True in let y = False in x \<and> y "
    assumes  e2 :  " let y = (1 :: nat) in y \<ge> (2 :: nat) "
    assumes  forallLet :  " (\<forall> (x :: R tensor). let y = x in (eqTensorReduced y (flextensor_from_vec [] [ (((1 :: R) )) ]))) "
    assumes  letForall :  " let y = (flextensor_from_vec [] [ (((1 :: R) )) ]) in (\<forall> (x :: R tensor). (eqTensorReduced y x)) "
    assumes  forallInLet :  " (forallInList (\<lambda>  x . (let y = x in y = (1 :: nat))) ((1 :: nat) # [])) "
    assumes  letForallIn :  " let y = (1 :: nat) in (forallInList (\<lambda>  x . (y = x)) ((1 :: nat) # [])) "
    assumes  correctOutput :  " let y = (flextensor_from_vec [] [ (((0 :: R) )) ]) in (falsey y) "

    begin

    end

end